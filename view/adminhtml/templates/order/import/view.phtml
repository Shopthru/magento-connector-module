<?php
/**
 * Template for viewing import log details
 *
 * @var \Shopthru\Connector\Block\Adminhtml\Order\Import\View $block
 */
$importLog = $block->getImportLog();
?>
    <div class="page-main-actions">
        <div class="page-actions">
            <div class="page-actions-buttons">
                <button id="back" title="<?= $block->escapeHtmlAttr(__('Back')) ?>" type="button" class="action-scalable back" onclick="location.href = '<?= $block->escapeUrl($block->getUrl('*/*/')) ?>';">
                    <span><?= $block->escapeHtml(__('Back')) ?></span>
                </button>
            </div>
        </div>
    </div>

    <section class="admin__page-section">
        <div class="admin__page-section-title">
            <span class="title"><?= $block->escapeHtml(__('Import Information')) ?></span>
        </div>
        <div class="admin__page-section-content">
            <div class="admin__page-section-item-content">
                <table class="admin__table-secondary">
                    <tbody>
                    <tr>
                        <th><?= $block->escapeHtml(__('Import ID')) ?>:</th>
                        <td><?= $block->escapeHtml($importLog->getImportId()) ?></td>
                    </tr>
                    <tr>
                        <th><?= $block->escapeHtml(__('Shopthru Order ID')) ?>:</th>
                        <td><?= $block->escapeHtml($importLog->getShopthruOrderId()) ?></td>
                    </tr>
                    <tr>
                        <th><?= $block->escapeHtml(__('Publisher Ref')) ?>:</th>
                        <td><?= $block->escapeHtml($importLog->getShopthruPublisherRef() ?: '') ?></td>
                    </tr>
                    <tr>
                        <th><?= $block->escapeHtml(__('Publisher Name')) ?>:</th>
                        <td><?= $block->escapeHtml($importLog->getShopthruPublisherName() ?: '') ?></td>
                    </tr>
                    <tr>
                        <th><?= $block->escapeHtml(__('Status')) ?>:</th>
                        <td><?= $block->escapeHtml($importLog->getStatus()) ?></td>
                    </tr>
                    <?php if ($importLog->getMagentoOrderId()): ?>
                        <tr>
                            <th><?= $block->escapeHtml(__('Magento Order #')) ?>:</th>
                            <td>
                                <a href="<?= $block->escapeUrl($block->getUrl('sales/order/view', ['order_id' => $importLog->getMagentoOrderId()])) ?>">
                                    <?= $block->escapeHtml($importLog->getMagentoOrderId()) ?>
                                </a>
                            </td>
                        </tr>
                    <?php endif; ?>
                    <tr>
                        <th><?= $block->escapeHtml(__('Imported At')) ?>:</th>
                        <td><?= $block->escapeHtml($block->formatDate($importLog->getImportedAt())) ?></td>
                    </tr>
                    <tr>
                        <th><?= $block->escapeHtml(__('Created At')) ?>:</th>
                        <td><?= $block->escapeHtml($block->formatDate($importLog->getCreatedAt())) ?></td>
                    </tr>
                    <?php if ($importLog->getFailedReason()): ?>
                        <tr>
                            <th><?= $block->escapeHtml(__('Failed Reason')) ?>:</th>
                            <td><?= $block->escapeHtml($importLog->getFailedReason()) ?></td>
                        </tr>
                    <?php endif; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </section>

<?php if ($importLog->getLogData()): ?>
    <section class="admin__page-section">
        <div class="admin__page-section-title">
            <span class="title"><?= $block->escapeHtml(__('Log Data')) ?></span>
        </div>
        <div class="admin__page-section-content">
            <div class="admin__page-section-item-content">
                <pre><?= $block->escapeHtml($block->formatJson($importLog->getLogData())) ?></pre>
            </div>
        </div>
    </section>
<?php endif; ?>

<?php if ($importLog->getAdditionalData()): ?>
    <section class="admin__page-section">
        <div class="admin__page-section-title">
            <span class="title"><?= $block->escapeHtml(__('Additional Data')) ?></span>
        </div>
        <div class="admin__page-section-content">
            <div class="admin__page-section-item-content">
                <pre><?= $block->escapeHtml($block->formatJson($importLog->getAdditionalData())) ?></pre>
            </div>
        </div>
    </section>
<?php endif; ?>
